#include<bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int t;
    int count = 0;
    cin>>t;
    while(t--){
        int n,x;
        cin>>n>>x;

        vector<int> door;
        for(int i = 0; i<n; i++){
            cin>>door[i];
        }
        for(int )
    }
    return 0;
}